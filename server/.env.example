# For running the Go server directly (without docker compose).
# Copy to .env and adjust to match your local Postgres.
POSTGRES_URL=postgres://postgres:mysecretpassword@localhost:5432/codedb?sslmode=disable
PORT=8080
CORS_ALLOWED_ORIGINS=*
